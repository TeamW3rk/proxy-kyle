config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 20
scenarios:
  - flow:
      - loop:
          - get:
              url: "/r/4/bookings"
        count: 50


# /r/4